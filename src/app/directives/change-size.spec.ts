import { ChangeSize } from './change-size';

describe('ChangeSize', () => {
  it('should create an instance', () => {
    const directive = new ChangeSize();
    expect(directive).toBeTruthy();
  });
});
